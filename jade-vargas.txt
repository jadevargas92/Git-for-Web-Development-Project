https://codepen.io/jade-vargas/pen/KKgrEwo

    1. What is Semantic HTML? 
    - Semantic HTML clearly defines its content and is more specific. Div and Spans are non semantic and doesn't clearly define its content.
    - Section, header, nav, article, footer, aside, they all clearly define the content.

    2. What is HTML used for?
    - Structuring the web page with content

    3. What is an attribute and where do we put it?
    - Attributes supply data to an element, we place it in the starting tag.

    4. What is the h1 tag used for? How many times should I use it on a page?
    - h1 tag is used for the title of the page or post and should only be used once.

    5. Name two tags that have required attributes
    - <img> and <a>

    6. What do we put in the head of our HTML document?
    -<meta> and <title>

    7. What is an id?
    - an attribute for a specific html element

    8. What elements can I add an id to?
    - all html element

    9. How many times can I use the same id on a page?
    - you are only supposed to add it once, but can add it to multiple elements (though highly frowned upon)

    10. What is a class? 
    - it specifies a class for html elements.

    11. What elements can I add a class to?
    - all elements

    12. How many times can I use the same class on a page?
    - As many times as you would like

    13. How do I get my link to open in a new tab?
    - target="_blank"

    14. What is the alt attribute in the image tag used for?
    - mainly for accessibility. It allows users to read/hear the description of the image on the page for blind users in alternative format.

    15. How do I reference an id?
    - #

    16. What is the difference between a section and a div
    - Semantics. Section is more descriptive that div.

    17. What is CSS used for? 
    - Styling HTML

    18. How to we select an element? Example - every h2 on the page
    - h2 { 

    }

    19. What is the difference between a class and an id? - Give me an example of when I might use each one
    -class allows you to target multiple html elements. id is met for a specific unique element. 
    - You may have a list of items where each item belongs to a certain class, but for the top item you might want to style it differently, so you would add a unique id.

    20. How do we select classes in CSS?
    - .some-class {

    }
    - .class 

    21. How do we select a p element with a single class of “human””?
    -<p class="human">Jade</p>

    22. What is a parent child selector? When would this be useful?
    - It provides more specificty when targeting an element to style, specifically the direct children of a parent element.
    - It would be useful when you want to target a specific child of a parent.
    = For example - If you only want to style the first sentence of a section.

    23. How do you select all links within a div with the class of sidebar?
    - div > a.sidebar {

    }

    24. What is a pseudo selector?
    - a selector that only happens when a certain state of the element is present. i.e. if you hover over an element, it can change the state of the element.

    25. What do we use the change the spacing between lines?
    - line-height

    26. What do we use to change the spacing between letters?
    - letter-spacing

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
    - CAPITALS you would use text-transform: uppercase
    - lowercase you use text-transform: lowercase
    - Capitalize you use text-transform: capitalize 

    28. How do I add a 1px border around my div that is dotted and black?
    - border: 1px dotted black;

    29. How do I select everything on the page? 
    - * {

    }

    30. How do I write a comment in CSS?
    - /* This is how to write a comment in css */ ( it can alsoe be done on multiple lines)

    31. How do I find out what file I am in, when I am using the command line? 
    - pwd

    32. Using the command line - how do I see a list of files/folders in my current folder?
    - ls

    33. How do I remove a file via the command line? Why do I have to be careful with this? 
    - rm - You have to be careful because you can't get that file back. It is not like deleting and moving to recycle bin. It is permanently deleted.

    34. Why should I use version control? 
    - First, it siumlates a real world environment. More important, it allows a collaboration of multiple engineers to work on a single project, and allows you to roll back
    the version if a bug is shipped.

    35. How often should I commit to github?
    - Often. No set rule, but the answer is often.

    36. What is the command we would use to push our repo up to github? 
    - git push -u origin branch-name

    37. Walk me through Lambda's git flow. 
     - 1. Fork the Lambda School repo on Git
     - 2. Clone (Download) your repo from git to your local machine.
     - 3. create your own branch for the repo, which will be the one you work on, by using git checkout -b 'yourFirstName-yourLastName'
     - 4. Work on your repo and do the project.
     - 5. git add . to add the changed to the staging area.
     - 6. git commit -m 'descriptive message about the changes' to commit your changes.
     - 7. git push -u origin branch-name (step 36) to push from your local machine up to github
     - 8. Go back to your forked copy, no Lambdas, on Git and make a pull request.
     - 9. I think we merge now? Pace said to merge, but the document says not to.
     - 10. Submit the link to canvas.

Stretch Questions

    1. What is the difference between an inline element and a block element?
    - With a block element, a new line will always be created following the tag.
    - With an inline element, there will never be a new line.

    2. What happens when an element is positioned absolutely?
    - It is positioned in relation to its parent element.

    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width?
    - 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    - Block Elements 1) Section, Nav, footer
    - Inline Elements 1) span 2) <a>
    - Inline-Block 1) Button
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 